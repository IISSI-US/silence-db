{
    "getAll": {
        "description": "Gets all entries from 'Usuarios'",
        "route": "/usuarios",
        "method": "GET",
        "sql": "SELECT * FROM Usuarios",
        "auth_required": false,
        "allowed_roles": [
            "*"
        ]
    },
    "getById": {
        "description": "Gets an entry from 'Usuarios' by its primary key",
        "route": "/usuarios/$usuarioId",
        "method": "GET",
        "sql": "SELECT * FROM Usuarios WHERE usuarioId = $usuarioId",
        "auth_required": false,
        "allowed_roles": [
            "*"
        ]
    },
    "create": {
        "description": "Creates a new entry in 'Usuarios'",
        "route": "/usuarios",
        "method": "POST",
        "sql": "INSERT INTO Usuarios (nombre, genero, edad, email) VALUES ($nombre, $genero, $edad, $email)",
        "request_body_params": [
            "nombre",
            "genero",
            "edad",
            "email"
        ],
        "auth_required": false,
        "allowed_roles": [
            "*"
        ]
    },
    "update": {
        "description": "Updates an existing entry in 'Usuarios' by its primary key",
        "route": "/usuarios/$usuarioId",
        "method": "PUT",
        "sql": "UPDATE Usuarios SET nombre = $nombre, genero = $genero, edad = $edad, email = $email WHERE usuarioId = $usuarioId",
        "request_body_params": [
            "nombre",
            "genero",
            "edad",
            "email"
        ],
        "auth_required": false,
        "allowed_roles": [
            "*"
        ]
    },
    "delete": {
        "description": "Deletes an existing entry in 'Usuarios' by its primary key",
        "route": "/usuarios/$usuarioId",
        "method": "DELETE",
        "sql": "DELETE FROM Usuarios WHERE usuarioId = $usuarioId",
        "auth_required": false,
        "allowed_roles": [
            "*"
        ]
    }
}