/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const group_enrollmentsAPI_auto = {

    /** Gets all entries from 'group_enrollments' */
    getAll: async function() {
        let response = await axios.get(`${BASE_URL}/group_enrollments`, requestOptions);
        return response.data;
    },

    /** Gets an entry from 'group_enrollments' by its primary key */
    getById: async function(student_id) {
        let response = await axios.get(`${BASE_URL}/group_enrollments/${student_id}`, requestOptions);
        return response.data[0];
    },

    /** Creates a new entry in 'group_enrollments' */
    create: async function(formData) {
        let response = await axios.post(`${BASE_URL}/group_enrollments`, formData, requestOptions);
        return response.data;
    },

    /** Updates an existing entry in 'group_enrollments' by its primary key */
    update: async function(formData, student_id) {
        let response = await axios.put(`${BASE_URL}/group_enrollments/${student_id}`, formData, requestOptions);
        return response.data;
    },

    /** Deletes an existing entry in 'group_enrollments' by its primary key */
    delete: async function(student_id) {
        let response = await axios.delete(`${BASE_URL}/group_enrollments/${student_id}`, requestOptions);
        return response.data;
    },
};

export { group_enrollmentsAPI_auto };