/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const teaching_loadsAPI_auto = {

    /** Gets all entries from 'teaching_loads' */
    getAll: async function() {
        let response = await axios.get(`${BASE_URL}/teaching_loads`, requestOptions);
        return response.data;
    },

    /** Gets an entry from 'teaching_loads' by its primary key */
    getById: async function(professor_id) {
        let response = await axios.get(`${BASE_URL}/teaching_loads/${professor_id}`, requestOptions);
        return response.data[0];
    },

    /** Creates a new entry in 'teaching_loads' */
    create: async function(formData) {
        let response = await axios.post(`${BASE_URL}/teaching_loads`, formData, requestOptions);
        return response.data;
    },

    /** Updates an existing entry in 'teaching_loads' by its primary key */
    update: async function(formData, professor_id) {
        let response = await axios.put(`${BASE_URL}/teaching_loads/${professor_id}`, formData, requestOptions);
        return response.data;
    },

    /** Deletes an existing entry in 'teaching_loads' by its primary key */
    delete: async function(professor_id) {
        let response = await axios.delete(`${BASE_URL}/teaching_loads/${professor_id}`, requestOptions);
        return response.data;
    },
};

export { teaching_loadsAPI_auto };