/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const credentialsAPI_auto = {

    /** Gets all entries from 'credentials' */
    getAll: async function() {
        let response = await axios.get(`${BASE_URL}/credentials`, requestOptions);
        return response.data;
    },

    /** Gets an entry from 'credentials' by its primary key */
    getById: async function(credential_id) {
        let response = await axios.get(`${BASE_URL}/credentials/${credential_id}`, requestOptions);
        return response.data[0];
    },

    /** Creates a new entry in 'credentials' */
    create: async function(formData) {
        let response = await axios.post(`${BASE_URL}/credentials`, formData, requestOptions);
        return response.data;
    },

    /** Updates an existing entry in 'credentials' by its primary key */
    update: async function(formData, credential_id) {
        let response = await axios.put(`${BASE_URL}/credentials/${credential_id}`, formData, requestOptions);
        return response.data;
    },

    /** Deletes an existing entry in 'credentials' by its primary key */
    delete: async function(credential_id) {
        let response = await axios.delete(`${BASE_URL}/credentials/${credential_id}`, requestOptions);
        return response.data;
    },
};

export { credentialsAPI_auto };