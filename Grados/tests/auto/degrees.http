
### This is an auto-generated test suite, it needs to be completed with valid data.
### These are not all tests you need, more of them should be created to evaluate the functional
### requirements of your project. These tests only test the CRUD endpoints of the entity.
### Silence is a DEAL research team project, more info about us in https://deal.us.es
@BASE = http://127.0.0.1:8080/api/v1

### Auxiliary query
### Positive test
### Test 00: Get all existing degrees
### This query is used in several of the below tests it should be executed first.
# @name degrees
GET {{BASE}}/degrees

### Login a(n) credentials and save the generated token 
### This token is used in several of the below tests it should be executed second.
# @name login
POST {{BASE}}/login 
Content-Type: application/json 

{ 
	"email": ###REPLACE###,
	"password": ###REPLACE###
}

###
@token = {{login.response.body.sessionToken}}


### TESTS BEGIN

### Test 01: Get one existing degrees by its id.
### positive test 
@degId = {{degrees.response.body.0.degree_id}}
GET {{BASE}}/degrees/{{degId}} 
Content-Type: application/json 


### Test 02: Try get one existing degrees by its nonexistent id.
### negative test 
GET {{BASE}}/degrees/999999999
Content-Type: application/json 


### Test 03: Add a new degrees successfully
### Positive test 
### We assume that the token has been aquired by the login request.
# @name newdegrees
POST {{BASE}}/degrees 
Content-Type: application/json 
Token: {{token}} 

{
	"degree_name": ###REPLACE###,
	"duration_years": ###REPLACE###
} 

### Check the created degrees

@newdegreesid = {{newdegrees.response.body.lastId}}
GET {{BASE}}/degrees/{{newdegreesid}}
Content-Type: application/json 

### Test 04: Add a new degrees without a session token
### Negative test 
POST {{BASE}}/degrees 
Content-Type: application/json 

{ 
	"degree_name": ###REPLACE###,
	"duration_years": ###REPLACE###
}

### Test 05: Modify an existing degrees 
### Positive test 
@degId = {{degrees.response.body.0.degree_id}} 
PUT {{BASE}}/degrees/{{degId}} 
Content-Type: application/json 
Token: {{token}} 

{ 
	"degree_name": ###REPLACE###,
	"duration_years": ###REPLACE###
}

### Check the modified degrees

GET {{BASE}}/degrees/{{newdegreesid}}
Content-Type: application/json 

### Test 06: Try to modify an existing degrees without a session token
### Negative test
@degId = {{degrees.response.body.0.degree_id}}
PUT {{BASE}}/degrees/{{degId}}
Content-Type: application/json

{
	"degree_name": ###REPLACE###,
	"duration_years": ###REPLACE###
}

### Test 07: Delete an existing degrees
### Positive test

### Create a new degrees, which we will delete
# @name createddegToDelete
POST {{BASE}}/degrees
Content-Type: application/json
Token: {{token}}

{
	"degree_name": ###REPLACE###,
	"duration_years": ###REPLACE###
}

### Check the created degrees
@degdelId = {{createddegToDelete.response.body.lastId}}
GET {{BASE}}/degrees/{{degdelId}}
Content-Type: application/json 

### Delete the degrees
DELETE {{BASE}}/degrees/{{degdelId}}
Token: {{token}}

### Check the deleted degrees
GET {{BASE}}/degrees/{{degdelId}}
Content-Type: application/json 

### Test 08: Try to delete a degrees without a session token
### Negative test
DELETE {{BASE}}/degrees/{{degId}}

