
### This is an auto-generated test suite, it needs to be completed with valid data.
### These are not all tests you need, more of them should be created to evaluate the functional
### requirements of your project. These tests only test the CRUD endpoints of the entity.
### Silence is a DEAL research team project, more info about us in https://deal.us.es
@BASE = http://127.0.0.1:8080/api/v1

### Auxiliary query
### Positive test
### Test 00: Get all existing group_enrollments
### This query is used in several of the below tests it should be executed first.
# @name group_enrollments
GET {{BASE}}/group_enrollments

### Login a(n) credentials and save the generated token 
### This token is used in several of the below tests it should be executed second.
# @name login
POST {{BASE}}/login 
Content-Type: application/json 

{ 
	"email": ###REPLACE###,
	"password": ###REPLACE###
}

###
@token = {{login.response.body.sessionToken}}


### TESTS BEGIN

### Test 01: Get one existing group_enrollments by its id.
### positive test 
@groId = {{group_enrollments.response.body.0.student_id}}
GET {{BASE}}/group_enrollments/{{groId}} 
Content-Type: application/json 


### Test 02: Try get one existing group_enrollments by its nonexistent id.
### negative test 
GET {{BASE}}/group_enrollments/999999999
Content-Type: application/json 


### Test 03: Add a new group_enrollments successfully
### Positive test 
### We assume that the token has been aquired by the login request.
# @name newgroup_enrollments
POST {{BASE}}/group_enrollments 
Content-Type: application/json 
Token: {{token}} 

{
	"group_id": ###REPLACE###
} 

### Check the created group_enrollments

@newgroup_enrollmentsid = {{newgroup_enrollments.response.body.lastId}}
GET {{BASE}}/group_enrollments/{{newgroup_enrollmentsid}}
Content-Type: application/json 

### Test 04: Add a new group_enrollments without a session token
### Negative test 
POST {{BASE}}/group_enrollments 
Content-Type: application/json 

{ 
	"group_id": ###REPLACE###
}

### Test 05: Modify an existing group_enrollments 
### Positive test 
@groId = {{group_enrollments.response.body.0.student_id}} 
PUT {{BASE}}/group_enrollments/{{groId}} 
Content-Type: application/json 
Token: {{token}} 

{ 
	"group_id": ###REPLACE###
}

### Check the modified group_enrollments

GET {{BASE}}/group_enrollments/{{newgroup_enrollmentsid}}
Content-Type: application/json 

### Test 06: Try to modify an existing group_enrollments without a session token
### Negative test
@groId = {{group_enrollments.response.body.0.student_id}}
PUT {{BASE}}/group_enrollments/{{groId}}
Content-Type: application/json

{
	"group_id": ###REPLACE###
}

### Test 07: Delete an existing group_enrollments
### Positive test

### Create a new group_enrollments, which we will delete
# @name createdgroToDelete
POST {{BASE}}/group_enrollments
Content-Type: application/json
Token: {{token}}

{
	"group_id": ###REPLACE###
}

### Check the created group_enrollments
@grodelId = {{createdgroToDelete.response.body.lastId}}
GET {{BASE}}/group_enrollments/{{grodelId}}
Content-Type: application/json 

### Delete the group_enrollments
DELETE {{BASE}}/group_enrollments/{{grodelId}}
Token: {{token}}

### Check the deleted group_enrollments
GET {{BASE}}/group_enrollments/{{grodelId}}
Content-Type: application/json 

### Test 08: Try to delete a group_enrollments without a session token
### Negative test
DELETE {{BASE}}/group_enrollments/{{groId}}

