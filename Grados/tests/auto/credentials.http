
### This is an auto-generated test suite, it needs to be completed with valid data.
### These are not all tests you need, more of them should be created to evaluate the functional
### requirements of your project. These tests only test the CRUD endpoints of the entity.
### Silence is a DEAL research team project, more info about us in https://deal.us.es
@BASE = http://127.0.0.1:8080/api/v1

### Auxiliary query
### Positive test
### Test 00: Get all existing credentials
### This query is used in several of the below tests it should be executed first.
# @name credentials
GET {{BASE}}/credentials

### Login a(n) credentials and save the generated token 
### This token is used in several of the below tests it should be executed second.
# @name login
POST {{BASE}}/login 
Content-Type: application/json 

{ 
	"email": ###REPLACE###,
	"password": ###REPLACE###
}

###
@token = {{login.response.body.sessionToken}}


### TESTS BEGIN

### Test 01: Get one existing credentials by its id.
### positive test 
@creId = {{credentials.response.body.0.credential_id}}
GET {{BASE}}/credentials/{{creId}} 
Content-Type: application/json 


### Test 02: Try get one existing credentials by its nonexistent id.
### negative test 
GET {{BASE}}/credentials/999999999
Content-Type: application/json 


### Test 03: Add a new credentials successfully
### Positive test 
### We assume that the token has been aquired by the login request.
# @name newcredentials
POST {{BASE}}/credentials 
Content-Type: application/json 
Token: {{token}} 

{
	"email": ###REPLACE###,
	"role": ###REPLACE###,
	"password_hash": ###REPLACE###
} 

### Check the created credentials

@newcredentialsid = {{newcredentials.response.body.lastId}}
GET {{BASE}}/credentials/{{newcredentialsid}}
Content-Type: application/json 

### Test 04: Add a new credentials without a session token
### Negative test 
POST {{BASE}}/credentials 
Content-Type: application/json 

{ 
	"email": ###REPLACE###,
	"role": ###REPLACE###,
	"password_hash": ###REPLACE###
}

### Test 05: Modify an existing credentials 
### Positive test 
@creId = {{credentials.response.body.0.credential_id}} 
PUT {{BASE}}/credentials/{{creId}} 
Content-Type: application/json 
Token: {{token}} 

{ 
	"email": ###REPLACE###,
	"role": ###REPLACE###,
	"password_hash": ###REPLACE###
}

### Check the modified credentials

GET {{BASE}}/credentials/{{newcredentialsid}}
Content-Type: application/json 

### Test 06: Try to modify an existing credentials without a session token
### Negative test
@creId = {{credentials.response.body.0.credential_id}}
PUT {{BASE}}/credentials/{{creId}}
Content-Type: application/json

{
	"email": ###REPLACE###,
	"role": ###REPLACE###,
	"password_hash": ###REPLACE###
}

### Test 07: Delete an existing credentials
### Positive test

### Create a new credentials, which we will delete
# @name createdcreToDelete
POST {{BASE}}/credentials
Content-Type: application/json
Token: {{token}}

{
	"email": ###REPLACE###,
	"role": ###REPLACE###,
	"password_hash": ###REPLACE###
}

### Check the created credentials
@credelId = {{createdcreToDelete.response.body.lastId}}
GET {{BASE}}/credentials/{{credelId}}
Content-Type: application/json 

### Delete the credentials
DELETE {{BASE}}/credentials/{{credelId}}
Token: {{token}}

### Check the deleted credentials
GET {{BASE}}/credentials/{{credelId}}
Content-Type: application/json 

### Test 08: Try to delete a credentials without a session token
### Negative test
DELETE {{BASE}}/credentials/{{creId}}

