
### This is an auto-generated test suite, it needs to be completed with valid data.
### These are not all tests you need, more of them should be created to evaluate the functional
### requirements of your project. These tests only test the CRUD endpoints of the entity.
### Silence is a DEAL research team project, more info about us in https://deal.us.es
@BASE = http://127.0.0.1:8080/api/v1

### Auxiliary query
### Positive test
### Test 00: Get all existing groups
### This query is used in several of the below tests it should be executed first.
# @name groups
GET {{BASE}}/groups

### Login a(n) credentials and save the generated token 
### This token is used in several of the below tests it should be executed second.
# @name login
POST {{BASE}}/login 
Content-Type: application/json 

{ 
	"email": ###REPLACE###,
	"password": ###REPLACE###
}

###
@token = {{login.response.body.sessionToken}}


### TESTS BEGIN

### Test 01: Get one existing groups by its id.
### positive test 
@groId = {{groups.response.body.0.group_id}}
GET {{BASE}}/groups/{{groId}} 
Content-Type: application/json 


### Test 02: Try get one existing groups by its nonexistent id.
### negative test 
GET {{BASE}}/groups/999999999
Content-Type: application/json 


### Test 03: Add a new groups successfully
### Positive test 
### We assume that the token has been aquired by the login request.
# @name newgroups
POST {{BASE}}/groups 
Content-Type: application/json 
Token: {{token}} 

{
	"subject_id": ###REPLACE###,
	"group_name": ###REPLACE###,
	"activity": ###REPLACE###,
	"academic_year": ###REPLACE###
} 

### Check the created groups

@newgroupsid = {{newgroups.response.body.lastId}}
GET {{BASE}}/groups/{{newgroupsid}}
Content-Type: application/json 

### Test 04: Add a new groups without a session token
### Negative test 
POST {{BASE}}/groups 
Content-Type: application/json 

{ 
	"subject_id": ###REPLACE###,
	"group_name": ###REPLACE###,
	"activity": ###REPLACE###,
	"academic_year": ###REPLACE###
}

### Test 05: Modify an existing groups 
### Positive test 
@groId = {{groups.response.body.0.group_id}} 
PUT {{BASE}}/groups/{{groId}} 
Content-Type: application/json 
Token: {{token}} 

{ 
	"subject_id": ###REPLACE###,
	"group_name": ###REPLACE###,
	"activity": ###REPLACE###,
	"academic_year": ###REPLACE###
}

### Check the modified groups

GET {{BASE}}/groups/{{newgroupsid}}
Content-Type: application/json 

### Test 06: Try to modify an existing groups without a session token
### Negative test
@groId = {{groups.response.body.0.group_id}}
PUT {{BASE}}/groups/{{groId}}
Content-Type: application/json

{
	"subject_id": ###REPLACE###,
	"group_name": ###REPLACE###,
	"activity": ###REPLACE###,
	"academic_year": ###REPLACE###
}

### Test 07: Delete an existing groups
### Positive test

### Create a new groups, which we will delete
# @name createdgroToDelete
POST {{BASE}}/groups
Content-Type: application/json
Token: {{token}}

{
	"subject_id": ###REPLACE###,
	"group_name": ###REPLACE###,
	"activity": ###REPLACE###,
	"academic_year": ###REPLACE###
}

### Check the created groups
@grodelId = {{createdgroToDelete.response.body.lastId}}
GET {{BASE}}/groups/{{grodelId}}
Content-Type: application/json 

### Delete the groups
DELETE {{BASE}}/groups/{{grodelId}}
Token: {{token}}

### Check the deleted groups
GET {{BASE}}/groups/{{grodelId}}
Content-Type: application/json 

### Test 08: Try to delete a groups without a session token
### Negative test
DELETE {{BASE}}/groups/{{groId}}

