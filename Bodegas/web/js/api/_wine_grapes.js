/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const wine_grapesAPI_auto = {

    /** Gets all entries from 'wine_grapes' */
    getAll: async function() {
        let response = await axios.get(`${BASE_URL}/wine_grapes`, requestOptions);
        return response.data;
    },

    /** Gets an entry from 'wine_grapes' by its primary key */
    getById: async function(wine_id) {
        let response = await axios.get(`${BASE_URL}/wine_grapes/${wine_id}`, requestOptions);
        return response.data[0];
    },

    /** Creates a new entry in 'wine_grapes' */
    create: async function(formData) {
        let response = await axios.post(`${BASE_URL}/wine_grapes`, formData, requestOptions);
        return response.data;
    },

    /** Updates an existing entry in 'wine_grapes' by its primary key */
    update: async function(formData, wine_id) {
        let response = await axios.put(`${BASE_URL}/wine_grapes/${wine_id}`, formData, requestOptions);
        return response.data;
    },

    /** Deletes an existing entry in 'wine_grapes' by its primary key */
    delete: async function(wine_id) {
        let response = await axios.delete(`${BASE_URL}/wine_grapes/${wine_id}`, requestOptions);
        return response.data;
    },
};

export { wine_grapesAPI_auto };